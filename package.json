{
  "name": "@standardagents/brolly-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm --filter @standardagents/brolly-core build && pnpm --filter @standardagents/brolly-notifiers build && pnpm --filter @standardagents/brolly-runtime build && pnpm --filter @standardagents/brolly-guard build && pnpm --filter @standardagents/brolly build",
    "deploy": "pnpm build && pnpm --filter @standardagents/brolly-guard db:migrate:remote && pnpm --filter @standardagents/brolly-guard deploy:built",
    "dev": "pnpm --filter @standardagents/brolly-guard dev",
    "test": "vitest run",
    "typecheck": "pnpm --filter @standardagents/brolly-core build && pnpm --filter @standardagents/brolly-notifiers build && pnpm -r typecheck"
  },
  "cloudflare": {
    "bindings": {
      "DB": { "description": "Brolly's D1 database. Cloudflare will provision it automatically." },
      "BROLLY_ACCOUNT_ID": { "description": "Filled automatically after the first Cloudflare sign-in. Leave the onboarding placeholder unchanged." },
      "BROLLY_CREDENTIAL_KEY": { "description": "Encrypts notification credentials at rest. Generate one with `openssl rand -base64 32`." },
      "BROLLY_OAUTH_CLIENT_ID": { "description": "Brolly's publisher-owned public Cloudflare OAuth client. Installers should not replace this value." },
      "BROLLY_OAUTH_REDIRECT_URI": { "description": "Brolly's verified OAuth relay. Installers should not replace this value." }
    }
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "@types/node": "^22.17.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.29.1"
  }
}
